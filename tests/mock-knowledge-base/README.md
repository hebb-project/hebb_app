---
tags: [type/meta]
---

# Mock knowledge base

Test fixture for `/api/vault/ingest`. ~10 markdown files, ~20
`[[wikilinks]]`. Realistic neuroscience/AI concepts so the demo graph
is meaningful when the connectome view shows it.

Don't import this from `core/` or `experiments/` — it is a fixture, not
application code.
